<article class="<%= ds_card_classes %> mb-8">
  <header class="mb-6">
    <h1 class="text-4xl font-bold text-text mb-4"><%= @post.title %></h1>
    <div class="flex items-center space-x-4 text-sm text-text-muted mb-4">
      <span>投稿者: <strong class="text-text"><%= @post.user.name %></strong></span>
      <span>|</span>
      <span>公開日: <%= @post.published_at&.strftime("%Y年%m月%d日") %></span>
    </div>

    <div id="like-section" class="flex items-center space-x-4 mb-6" data-controller="like" data-like-post-id-value="<%= @post.id %>">
      <div id="like-button-container">
        <% if @liked && @current_like %>
          <%= button_to "❤️ いいね済み", post_like_path(@post, @current_like),
              method: :delete,
              remote: true,
              class: ds_button_classes(:danger),
              id: "like-button",
              data: {
                post_id: @post.id,
                like_target: "button"
              } %>
        <% else %>
          <%= button_to "🤍 いいね", post_likes_path(@post),
              method: :post,
              remote: true,
              class: ds_button_classes(:secondary),
              id: "like-button",
              data: {
                post_id: @post.id,
                like_target: "button"
              } %>
        <% end %>
      </div>
      <span id="likes-count" class="font-semibold text-text-muted" data-like-target="count">
        <%= @likes_count %>件のいいね
      </span>
    </div>

    <% if user_signed_in? && (current_user == @post.user || current_user.admin?) %>
      <div class="flex space-x-4">
        <%= link_to "編集", edit_post_path(@post), class: ds_button_classes(:link) %>
        <span>|</span>
        <%= link_to "削除", post_path(@post), data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" }, class: "text-danger hover:text-danger-hover font-medium transition-colors" %>
      </div>
    <% end %>
  </header>

  <div class="markdown-content prose prose-lg max-w-none mt-6">
    <%= markdown_to_html(@post.body) %>
  </div>
</article>

<section class="<%= ds_card_classes %>">
  <h2 class="text-2xl font-bold text-text mb-6">コメント</h2>

  <% if @post.comments.any? %>
    <div class="space-y-4 mb-8">
      <% @post.comments.order(created_at: :asc).each do |comment| %>
        <div class="border border-border rounded-lg p-4 hover:border-border-subtle transition-colors">
          <div class="flex items-center justify-between mb-2">
            <p class="font-semibold text-text">
              <%= comment.author_name %>
            </p>
            <span class="text-sm text-text-muted">
              <%= comment.created_at.strftime("%Y年%m月%d日 %H:%M") %>
            </span>
          </div>
          <p class="text-text-muted mt-3"><%= simple_format(comment.content) %></p>
          <% if user_signed_in? && (current_user == comment.user || current_user.admin?) %>
            <div class="mt-3">
              <%= link_to "削除", post_comment_path(@post, comment),
                  data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" },
                  class: "text-danger hover:text-danger-hover text-sm font-medium transition-colors" %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-text-muted mb-8">まだコメントがありません。</p>
  <% end %>

  <div class="border-t border-border pt-6">
    <h3 class="text-xl font-semibold text-text mb-4">コメントを投稿</h3>
    <%= form_with model: [@post, @post.comments.build], local: true, class: "space-y-4" do |f| %>
      <% if @post.comments.build.errors.any? %>
        <div id="error_explanation" class="bg-danger-bg border-l-4 border-danger p-4 mb-4 text-danger-text">
          <ul class="list-disc list-inside">
            <% @post.comments.build.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <% unless user_signed_in? %>
        <%= render "shared/form_field", form: f, field_name: :guest_name, label_text: "お名前", input_html: f.text_field(:guest_name, required: true, class: ds_input_classes) %>
      <% end %>

      <%= render "shared/form_field", form: f, field_name: :content, label_text: "コメント", input_html: f.text_area(:content, rows: 5, required: true, class: "#{ds_input_classes} max-w-2xl") %>

      <div class="actions">
        <%= f.submit "コメントを投稿", class: ds_button_classes(:primary) %>
      </div>
    <% end %>
  </div>
</section>

<div class="mt-8">
  <%= link_to "一覧に戻る", posts_path, class: ds_button_classes(:link) %>
</div>
