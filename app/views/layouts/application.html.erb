<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "My Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-surface <%= yield :body_class %>"<% if content_for?(:body_data_controller) %> data-controller="<%= yield :body_data_controller %>"<% end %>>
    <nav class="nav-bar border-b border-border shadow-sm">
      <div class="layout-container px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <% if content_for?(:post_form_appbar) %>
            <%= yield :post_form_appbar %>
          <% else %>
            <div class="flex items-center">
              <h1 class="text-xl font-bold">
                <%= link_to "MyBlogApp", root_path, class: "text-text hover:text-primary transition-colors" %>
              </h1>
            </div>
            <div class="flex items-center space-x-4">
              <% if Rails.env.development? %>
                <%= link_to "デザインシステム", design_system_path, class: "text-text-muted hover:text-primary transition-colors" %>
              <% end %>
              <% if user_signed_in? %>
                <div class="nav-account-wrap" data-controller="dropdown">
                  <button type="button" class="nav-account-trigger" data-dropdown-target="trigger" data-action="click->dropdown#toggle" aria-haspopup="true" aria-expanded="false" aria-label="アカウントメニュー">
                    <%= current_user.name %> <span aria-hidden="true">▼</span>
                  </button>
                  <div class="nav-dropdown-panel" data-dropdown-target="menu" role="menu">
                    <%= link_to "自分の記事", my_posts_path, role: "menuitem", class: "block" %>
                    <% if can_create_post? %>
                      <%= link_to "新規投稿", new_post_path, role: "menuitem", class: "block" %>
                    <% end %>
                    <%= link_to "ログアウト", destroy_user_session_path, role: "menuitem", data: { turbo_method: :delete }, class: "block" %>
                  </div>
                </div>
              <% else %>
                <%= link_to "ログイン", new_user_session_path, class: ds_button_classes(:link) %>
                <%= link_to "サインアップ", new_user_registration_path, class: ds_button_classes(:primary) %>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <%= render "shared/alert", type: :notice, message: notice %>
    <%= render "shared/alert", type: :alert, message: alert %>

    <div class="layout-container px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </div>
  </body>
</html>
